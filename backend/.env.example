PORT=8000
JWT_SECRET=change-this-jwt-secret
ADMIN_TOKEN=change-this-admin-token
BACKEND_PUBLIC_URL=http://localhost:8000
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
FACEBOOK_APP_ID=
FACEBOOK_APP_SECRET=
# Optional: if set, PostgreSQL is used instead of SQLite.
# DATABASE_URL=postgres://user:pass@localhost:5432/zito

# Optional: external price API (ArtikliApi) used by /price/check before local DB fallback.
# EXTERNAL_PRICES_API_BASE=http://SERVER_IP:1880
# EXTERNAL_PRICES_API_PATH=/api/artikli
# EXTERNAL_PRICES_TIMEOUT_MS=9000
# PRICE_REFRESH_HOUR_LOCAL=7
# PRICE_REFRESH_TIMEZONE=Europe/Skopje

# Optional: Loyalty SOAP service
# LOYALTY_SOAP_URL=http://SERVER/IBRestKartKor/Service1.svc
